{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default window permissions; also grants IPC to the hosted app so the real terminal works in production.",
  "windows": ["main"],
  "remote": { "urls": ["https://idaevia.app/*", "https://*.idaevia.app/*", "http://localhost:*/*"] },
  "permissions": ["core:default"]
}
