{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "No native permissions: the hosted platform uses authenticated HTTPS APIs only.",
  "windows": [
    "main"
  ],
  "permissions": []
}
