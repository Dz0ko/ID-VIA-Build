{
  "paths": {
    "canvases": "./designs"
  },
  "scripts": {
    "install": "cd .. && npm install && npx prisma generate && npx prisma db push && cd tempo && npm install",
    "start": "npm run dev"
  },
  "apps": [
    {
      "start": "npx next dev -p 3737",
      "url": "http://localhost:3737"
    }
  ]
}
